import { ListboxOptionDirective } from './listbox-option.directive';

describe('ListboxOptionDirective', () => {
  it('should create an instance', () => {
    const directive = new ListboxOptionDirective();
    expect(directive).toBeTruthy();
  });
});
